# Managed by harmonia-monad operator-rc-profile.
case ":$PATH:" in
  *:/fulcrum:*) ;;
  *) export PATH="/fulcrum:$PATH" ;;
esac
case ":$PATH:" in
  *:$HOME/.local/bin:*) ;;
  *) export PATH="$HOME/.local/bin:$PATH" ;;
esac
case ":$PATH:" in
  *:$HOME/bin:*) ;;
  *) export PATH="$HOME/bin:$PATH" ;;
esac
