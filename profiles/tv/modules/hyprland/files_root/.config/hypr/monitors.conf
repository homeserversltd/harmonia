# TV appliance output policy.
# Lock the active display to 1080p when the sink advertises it; Hyprland will apply it on reload/session start.
monitor = , 1920x1080@60, 0x0, 1.2
monitor = Unknown-1, disabled
