#!/usr/bin/env bash
set -u -o pipefail

exec sudo -n /usr/local/sbin/agathodaimon/cli.py gui update-modal || {
    if command -v notify-send >/dev/null 2>&1; then
        notify-send "Harmonia updates" "Could not open the update window."
    fi
    exit 1
}
