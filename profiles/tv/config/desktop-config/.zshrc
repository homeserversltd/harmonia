# CachyOS config loads Powerlevel10k. Disabled so only personal config runs.
# source /usr/share/cachyos-zsh-config/cachyos-config.zsh

# Personal config
[ -f ~/.zshrc.arch-install ] && source ~/.zshrc.arch-install
