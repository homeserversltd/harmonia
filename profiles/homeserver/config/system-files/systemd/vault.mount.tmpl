[Unit]
Description={{vault_mount_description}}
DefaultDependencies=no
Conflicts=umount.target
Before=umount.target

[Mount]
What=/dev/mapper/{{vault_mapper_name}}
Where={{vault_mount_path}}
Options=defaults

[Install]
WantedBy=multi-user.target
