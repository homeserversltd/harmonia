# samba

Visible HOMESERVER public scaffold for the `samba` Chrysalis concern phase.

Samba native appliance/NAS config; not a portal tab.

This folder is desired-state/config authority only. It installs nothing by itself, carries no secrets, and is consumed later by a single Chrysalis phase/unit for `samba`.
