# searx

Visible HOMESERVER public scaffold for the `searx` module.

SearXNG is included in the public homeserver profile as a normal module folder, not under a separate apps tree.

This folder is desired-state/config authority only. It installs nothing by itself, carries no secrets, and is consumed later by a single Chrysalis phase/unit for `searx`.
