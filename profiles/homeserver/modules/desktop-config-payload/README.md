# desktop-config-payload

This module owns the one canonical operator dotfile pool for Debian backend bodies. It converges the same `.zshrc`, `.aliases`, `.functions`, and `.profile` bytes to `/home/owner` with `backup_existing: true`. It is not an Arch profile source.
