[Unit]
Description=Vault Mount Point
DefaultDependencies=no
Conflicts=umount.target
Before=umount.target

[Mount]
What=/dev/mapper/vault
Where=/vault
Options=defaults

[Install]
WantedBy=multi-user.target
