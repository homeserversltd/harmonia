when:
  event:
    - push

clone:
  git:
    image: docker.io/woodpeckerci/plugin-git:2.8.0
    settings:
      custom-ssl-path: /etc/ssl/certs/ca-certificates.crt

steps:
  - name: cargo-test
    image: rust:1.85-bookworm
    commands:
      - cargo test --all-targets --locked --features test-facade

  - name: registry-pen-harmonia
    image: python:3.12-alpine
    backend_options:
      docker:
        user: "0"
    when:
      - event: push
        branch: main
    environment:
      KETHER_URL:
        from_secret: KETHER_URL
    commands:
      - |
        set -eu
        KETHER_ENDPOINT="${KETHER_URL:-}"
        KETHER_ENDPOINT="${KETHER_ENDPOINT%/}"
        if [ -z "$KETHER_ENDPOINT" ]; then
          KETHER_ENDPOINT="http://192.168.123.1:7100"
        fi
        export KETHER_ENDPOINT
        python3 - <<'PY'
        import http.client
        import json
        import os
        import sys
        import time
        import urllib.error
        import urllib.request

        RETRY_STATUSES = {502, 503, 504}
        MAX_ATTEMPTS = 5
        BACKOFF_SECONDS = (2, 5, 10, 20, 30)

        def post_json_with_retry(url, payload, label):
            data = json.dumps(payload).encode("utf-8")
            last_status = None
            last_raw = b""
            last_error = None
            for attempt in range(1, MAX_ATTEMPTS + 1):
                request = urllib.request.Request(
                    url, data=data, headers={"Content-Type": "application/json"}, method="POST",
                )
                print(f"{label}: attempt {attempt}/{MAX_ATTEMPTS} POST {url}")
                try:
                    with urllib.request.urlopen(request, timeout=1800) as response:
                        status = response.status
                        raw = response.read()
                except urllib.error.HTTPError as exc:
                    status = exc.code
                    raw = exc.read()
                except (urllib.error.URLError, http.client.RemoteDisconnected, TimeoutError, OSError) as exc:
                    status = None
                    raw = b""
                    last_error = str(exc)
                else:
                    last_error = None
                last_status = status
                last_raw = raw
                if status is not None and 200 <= status < 300:
                    return status, raw
                retryable = status in RETRY_STATUSES or status is None
                if not retryable or attempt == MAX_ATTEMPTS:
                    break
                delay = BACKOFF_SECONDS[min(attempt - 1, len(BACKOFF_SECONDS) - 1)]
                print(f"{label}: retrying after {delay}s; last_status={status}; last_error={last_error or ''}")
                time.sleep(delay)
            if last_error:
                print(f"{label}: last_transport_error={last_error}")
            return last_status if last_status is not None else 0, last_raw

        child_repo = "harmonia"
        endpoint = os.environ["KETHER_ENDPOINT"].rstrip("/")
        payload = {
            "child_repo": child_repo,
            "new_head": os.environ.get("CI_COMMIT_SHA", ""),
            "cause": os.environ.get("CI_PIPELINE_URL", ""),
        }
        print(f"{child_repo} registry-pen: POST {endpoint}/v1/registry-pen/stamp")
        status, raw = post_json_with_retry(endpoint + "/v1/registry-pen/stamp", payload, f"{child_repo} registry-pen")
        print(f"{child_repo} registry-pen: http_status={status}")
        if raw:
            print("response_body first 4096 bytes=")
            sys.stdout.write(raw[:4096].decode("utf-8", errors="replace"))
            print()
        if status < 200 or status >= 300:
            raise SystemExit(f"{child_repo} registry-pen: Kether stamp failed with HTTP {status}")
        print(f"{child_repo} registry-pen: stamped by Kether/Azoth blood lane")
        PY

  - name: build-publish-harmonia-package
    image: rust:1-bookworm
    when:
      - event: push
        branch: main
    environment:
      FORGEJO_TOKEN:
        from_secret: FORGEJO_TOKEN
    commands:
      - |
        set -eu
        command -v curl >/dev/null || { echo "build-publish-harmonia-package: curl is required in rust:1-bookworm" >&2; exit 1; }

        FORGEJO_TOKEN="$(printf '%s' "$${FORGEJO_TOKEN:-}" | tr -d '[:space:]')"
        if [ -z "$FORGEJO_TOKEN" ] && [ -r /mnt/keyexchange/FORGEJO_TOKEN ] && [ -s /mnt/keyexchange/FORGEJO_TOKEN ]; then
          FORGEJO_TOKEN="$(tr -d '[:space:]' < /mnt/keyexchange/FORGEJO_TOKEN)"
        fi
        if [ -z "$FORGEJO_TOKEN" ]; then
          printf '%s\n' "build-publish-harmonia-package: org secret injected empty and /mnt/keyexchange/FORGEJO_TOKEN absent — run keyman transmit forgejo-token --to homeserver-ci-agent" >&2
          exit 1
        fi
        if ! printf "%s" "$${CI_COMMIT_SHA:-}" | grep -Eq "^[0-9a-f]{40}$"; then
          echo "build-publish-harmonia-package: CI_COMMIT_SHA must be a full 40-hex commit SHA" >&2
          exit 1
        fi
        if [ -z "$${CI_PIPELINE_URL:-}" ]; then
          echo "build-publish-harmonia-package: CI_PIPELINE_URL is required" >&2
          exit 1
        fi

        HARMONIA_BUILD_SHA="$CI_COMMIT_SHA" cargo build --release
        target_dir="$(cargo metadata --format-version 1 --no-deps | sed -n 's/.*"target_directory":"\([^"]*\)".*/\1/p')"
        if [ -z "$target_dir" ] || [ ! -d "$target_dir/release" ]; then
          echo "build-publish-harmonia-package: Cargo release target directory is unavailable" >&2
          exit 1
        fi
        release_dir="$target_dir/release"
        candidates="$(find "$release_dir" -maxdepth 1 -type f -perm -111 -print)"
        candidate_count="$(printf '%s\n' "$candidates" | sed '/^$/d' | wc -l | tr -d '[:space:]')"
        if [ "$candidate_count" != "1" ]; then
          echo "build-publish-harmonia-package: expected exactly one top-level executable in $release_dir, found $candidate_count" >&2
          exit 1
        fi
        binary="$candidates"
        binary_sha256="$(sha256sum "$binary" | cut -d " " -f 1)"
        built_at="$(date -u +%Y-%m-%dT%H:%M:%SZ)"

        printf '{\n  "schema": "estate.artifact.manifest.v1",\n  "component": "harmonia",\n  "source_sha": "%s",\n  "target": "x86_64-unknown-linux-gnu",\n  "sha256": "%s",\n  "built_at": "%s",\n  "pipeline_url": "%s"\n}\n' \
          "$CI_COMMIT_SHA" "$binary_sha256" "$built_at" "$CI_PIPELINE_URL" \
          > manifest.json

        package_base="https://git.home.arpa/api/packages/HOMESERVERSLTD/generic/harmonia/$CI_COMMIT_SHA"
        auth_header="Authorization: token $FORGEJO_TOKEN"

        verify_existing_manifest() {
          existing_status="$(curl --silent --show-error \
            --output existing-manifest.json \
            --write-out "%{http_code}" \
            --header "$auth_header" \
            "$package_base/manifest.json")"
          if [ "$existing_status" != "200" ]; then
            echo "build-publish-harmonia-package: collision but existing manifest returned HTTP $existing_status" >&2
            exit 1
          fi
          existing_sha256="$(grep -Eo '"sha256"[[:space:]]*:[[:space:]]*"[0-9a-f]{64}"' existing-manifest.json | grep -Eo '[0-9a-f]{64}' || true)"
          if ! printf "%s" "$existing_sha256" | grep -Eq "^[0-9a-f]{64}$"; then
            echo "build-publish-harmonia-package: existing manifest has no unique 64-hex sha256" >&2
            exit 1
          fi
          if [ "$existing_sha256" != "$binary_sha256" ]; then
            echo "build-publish-harmonia-package: immutable version collision: existing sha256 does not match fresh build" >&2
            exit 1
          fi
          echo "build-publish-harmonia-package: immutable version already exists with matching sha256"
        }

        upload_file() {
          local_path="$1"
          remote_name="$2"
          content_type="$3"
          response_file="upload-$remote_name.response"
          upload_status="$(curl --silent --show-error \
            --output "$response_file" \
            --write-out "%{http_code}" \
            --request PUT \
            --header "$auth_header" \
            --header "Content-Type: $content_type" \
            --upload-file "$local_path" \
            "$package_base/$remote_name")"
          case "$upload_status" in
            200|201|204)
              echo "build-publish-harmonia-package: uploaded $remote_name (HTTP $upload_status)"
              ;;
            409)
              echo "build-publish-harmonia-package: $remote_name already exists; verifying immutable manifest"
              verify_existing_manifest
              ;;
            *)
              echo "build-publish-harmonia-package: upload of $remote_name failed with HTTP $upload_status" >&2
              if [ -s "$response_file" ]; then
                head -c 4096 "$response_file" >&2
                printf "\\n" >&2
              fi
              exit 1
              ;;
          esac
        }

        upload_file "$binary" harmonia application/octet-stream
        upload_file manifest.json manifest.json application/json
