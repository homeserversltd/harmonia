{
  "schema": "harmonia.module.v1",
  "id": "identity",
  "description": "Read HomeConsole identity through the generic command primitive.",
  "steps": [
    {
      "id": "uname",
      "tool": "command",
      "action": "run",
      "command": "/usr/bin/uname",
      "args": ["-a"]
    }
  ]
}
