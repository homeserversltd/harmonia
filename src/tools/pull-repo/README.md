# pull-repo

`pull-repo` is a registered Harmonia tool declaration resolved through `src/tools/index.json`, the single registry. `pull-repo` is one of the thirteen deed declaration records in `src/tools/index.json`: permutation `acquire`, deed `pull-repo`, permission `mutate`, and phases `observe → compare → act → attest`. It acquires the declared source through the keyed transactional do atom.

The two required mutation keys are diff-minted `Authorization` and the exact `--apply-or-timer` invocation key.
