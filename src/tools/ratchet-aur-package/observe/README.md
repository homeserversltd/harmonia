# observe

Reads and validates the declared AUR pin, verifies artifact-lock integrity, observes installed and upstream state, and derives the typed verdict. Upstream movement is a nudge and never action authority.
