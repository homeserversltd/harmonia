# set-clock

Converges household timezone and time synchronization. Observe uses read-only atoms; a different clock state alone mints both keys for act; report-home attests without replacing the legacy receipt.
