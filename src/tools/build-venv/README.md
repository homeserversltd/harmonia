# build-venv

`build-venv` is a registered Harmonia tool declaration resolved through `src/tools/index.json`, the single registry. `build-venv` is one of the thirteen deed declaration records in `src/tools/index.json`: permutation `converge`, deed `build-venv`, permission `mutate`, and phases `observe → compare → act → attest`. It converges the declared virtual environment through the keyed transactional do atom.

The two required mutation keys are diff-minted `Authorization` and the exact `--apply-or-timer` invocation key.
