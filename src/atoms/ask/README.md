# Ask

Ask performs bounded observations without mutation: file bytes and SHA-256, read-only standard-library commands, systemd state queries, and bounded HTTP probes. Ask does not block the engine path and does not mint mutation authority.
