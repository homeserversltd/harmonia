# Do

Do is the only mutation vocabulary. It contains twenty true-named transactional atoms, with one folder per atom. Each atom requires diff-minted `Authorization` and the exact `--apply-or-timer` invocation key by value.

Each deed reports a truthful outcome and follows the shared transactional rules, including backup-first file writes where the deed changes an existing file. Do never mints authority locally.
